"use client";
import {useEffect,useState} from "react";
import {Input} from "@/components/ui/input";
import {Button} from "@/components/ui/button";
export default function Setup(){const[key,setKey]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");useEffect(()=>{const p=new URLSearchParams(window.location.hash.slice(1));const k=p.get("setup");if(k){setKey(k);history.replaceState(null,"","/owner")}},[]);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch("/api/owner/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({key})});const p=await r.json();if(!r.ok)throw new Error(p.error);location.reload()}catch(e){setError(e instanceof Error?e.message:"Aktivasi belum berhasil.")}finally{setBusy(false)}}return <main className="owner-page"><div className="setup"><p className="owner-eyebrow">DILLA &amp; BOBBY</p><h1>Dashboard pemilik</h1><p>Aktifkan sekali untuk menghubungkan dashboard dengan akun yang sedang kamu gunakan.</p><form onSubmit={submit}><label htmlFor="setup-key">Kode aktivasi</label><Input id="setup-key" type="password" value={key} onChange={e=>setKey(e.target.value)} required autoComplete="off"/><Button disabled={busy}>{busy?"Mengaktifkan…":"Aktifkan Dashboard Saya"}</Button><p role="status">{error}</p></form></div></main>}
